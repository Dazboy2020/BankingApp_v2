import { Stack } from '@mui/material';

function TestSection() {
	return (
		<Stack
			sx={{
				minHeight: '100vh',
				backgroundColor: 'white',
			}}
		></Stack>
	);
}

export default TestSection;

// const combinedTransactions = state.expenses
// 		.concat(state.deposits)
// 		.map((item) => item.date);

// 	const formattedDates = combinedTransactions.map((date) => new Date(date));

// 	formattedDates.sort((a, b) => a - b);

// 	// Convert the sorted dates back to the original format
// 	const sortedDates = formattedDates.map((date) => date.toDateString());

// 	// Display the sorted dates
// 	console.log(sortedDates);
